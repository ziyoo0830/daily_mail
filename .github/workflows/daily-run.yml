on:
  schedule:
    # 每天 UTC 时间 12:00 执行（北京时间 08:00）
    # Cron 表达式：分 时 日 月 星期
    - cron: '0 12 * * *'
  
  # 允许手动触发工作流
  workflow_dispatch:

jobs:
  run-scripts:
    runs-on: windows-latest
    
    steps:
      # 1. 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 设置 Python 环境（启用缓存）
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      # 3. 安装依赖包
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. 运行脚本（每个脚本独立步骤）
      - name: Run zhishuv5.py
        env:
          PYTHONIOENCODING: utf-8
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          chcp 65001
          python zhishuv5.py

      - name: Run zhishuv6.py
        env:
          PYTHONIOENCODING: utf-8
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          chcp 65001
          python zhishuv6.py

      - name: Run cb.py
        env:
          PYTHONIOENCODING: utf-8
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          chcp 65001
          python cb.py

      - name: Run zhimingganggu_analyze_v3.py
        env:
          PYTHONIOENCODING: utf-8
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          chcp 65001
          python zhimingganggu_analyze_v3.py

      - name: Run zhimingganggu_analyze_v5.py
        env:
          PYTHONIOENCODING: utf-8
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          chcp 65001
          python zhimingganggu_analyze_v5.py
      # 5. 上传生成的文件（如果有输出文件）
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: output-files-${{ github.run_number }}
          path: |
            *.xlsx
            *.csv
            *.txt
          retention-days: 7
